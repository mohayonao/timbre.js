for items in categories
  if indexes[items.key] && indexes[items.key].length
    h2 #{items.caption}
    ul
      each o in indexes[items.key]
        li
          a(href=o.url) #{o.name}
          if o.dev
            span(class="label label-important") dev
